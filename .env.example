# EarningsLens environment-variable template
#
# The checked Python pipeline and the deployed static Next.js dashboard do not
# currently require environment variables. Keep local secrets in an ignored
# `.env` file if a future data source or gated model introduces credentials.
# Never expose private values through a NEXT_PUBLIC_* variable.
